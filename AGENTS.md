# AGENTS.md — Admissions Agent Tool 工作流程

當你（Agent）被載入此專案時，請嚴格依照以下流程執行。

---

## 🟢 Phase 0：初始化

### Step 0.1：載入說明
- 讀取 `README.md`，了解專案架構與目標

### Step 0.2：檢查環境
```bash
python --version  # 須 ≥ 3.8
pip show python-pptx 2>$null; if ($?) { "已安裝" } else { pip install python-pptx }
```

### Step 0.3：檢查 profile.md 狀態
- 讀取 `profile.md`
- 對照下方清單，記錄哪些欄位已填（有內容）、哪些未填（仍為 `{{PLACEHOLDER}}`）
- 將此狀態寫入記憶，後續每個 Phase 結束時更新

### Step 0.4：檢查 images/ 目錄
- 列出 `images/` 下所有檔案
- 記錄有哪些圖片可用

### Step 0.5：檢查 existing_ppt/ 目錄（現有 PPT）
列出 `existing_ppt/` 下的所有 `.pptx` 檔案。
如果存在現有 PPT，依照以下流程處理：

1. 問使用者：「existing_ppt/ 下有現成 PPT，你要怎麼處理？
   a) **當參考**：讓我讀取內容，作為問卷和模板的參考依據
   b) **直接沿用**：直接拿來用，不用重新產生
   c) **當起點**：讀取內容後，我在 build_ppt.py 裡重建它」

2. 如果使用者選 a) 或 c)，用 python-pptx 讀取現有 PPT：
   ```python
   from pptx import Presentation
   prs = Presentation("existing_ppt/檔名.pptx")
   for i, slide in enumerate(prs.slides):
       for shape in slide.shapes:
           if shape.has_text_frame:
               text = shape.text_frame.text
               # 記錄文字內容
           if shape.has_image:
               # 記錄圖片檔名（如果可取得）
   ```
   將萃取結果寫入 `existing_ppt/reference_content.md`

3. 萃取的文字內容可作為：
   - 問卷時提示使用者補充細節的參考
   - Phase 3 修改 build_ppt.py 時的內容參考

---

## 📚 A-D 分類參考（台灣技職備審系統）

在開始問卷前，先了解要產出的 PPT 有哪些類別。這些是台灣四技二專推甄的 A-D 備審分類。
使用者不一定每個類別都需要，問卷時請確認他要哪些。

| 類別 | 代號 | 內容簡述 | 所需資料 |
|------|------|----------|----------|
| A | A | 個人基本資料（學歷、自傳） | 基本身份（通常系統自動帶入，可不做 PPT） |
| B-1 | B-1a | **專題實作**：主打作品完整介紹 | 專題資料（章節 3-9） |
| B-1 | B-1b | **實習科目成果**：車/銑/鉗工、焊接、電學實習 | 實習照片、心得、職場學習 |
| B-2 | B-2a | **手繪製圖**：三視圖、剖面圖、等角圖 | 手繪作品照片 |
| B-2 | B-2b | **電腦輔助製圖**：AutoCAD / Inventor | CAD 作品截圖 |
| B-2 | B-2c | **3D 建模作品**：其他建模作品 | 3D 模型截圖 |
| C-1 | C-1 | **彈性學習時間成果** | 自主學習內容、成果照片 |
| C-5 | C-5 | **競賽表現** | 競賽獎項（章節 10） |
| C-7 | C-7a | **專業檢定證照** | 證照（章節 11） |
| C-7 | C-7b | **其他證照**（語言/資訊等） | 證照（章節 11） |
| C-8 | C-8 | **特殊優良表現證明** | 課外活動（章節 12） |
| D-1 | D-1 | **多元表現綜整心得** | 全部資料統整 |
| D-2 | D-2 | **學習歷程自述**（就讀動機+未來規劃） | 就讀動機（章節 13） |
| D-3 | D-3 | **其他有利審查資料** | 其他作品（章節 14） |

### 問卷時的對應

- 問卷 Round 3（主打專題）→ 主要對應 B-1a
- 問卷 Round 4（競賽獎項）→ 主要對應 C-5
- 問卷 Round 5（證照與課外）→ 主要對應 C-7、C-8
- 問卷 Round 6（就讀動機）→ 主要對應 D-2
- 實習科目（B-1b）→ Round 3 完成後，加問一輪實習經驗

---

## 🟡 Phase 1：六輪深度問答

### 通則

1. **只問你需要的欄位**：如果 profile.md 某欄位已填，跳過該問題
2. **一次一輪**：每輪結束後立即更新 profile.md
3. **追問細節**：如果使用者的回答太簡短，請追問
4. **記錄每輪進度**：每完成一輪，在 profile.md 頂部更新完成狀態
5. **不要同時問太多**：每輪最多 5-8 題

---

### Round 1 — 基本身份（5 題）

問完後更新 profile.md 第一章節。

題目：
1. 「你的中文姓名是什麼？（含姓氏和名字）」
2. 「你的英文姓名是什麼？（護照拼音或你常用的拼法）」
3. 「你的學校全名、科系、年級？」
4. 「你的 Email 和 GitHub 帳號？」
5. 「你有沒有個人使用的大頭照或照片？檔名是什麼？」

### Round 2 — 目標校系（5-7 題）

問完後更新 profile.md 第二章節。

題目：
1. 「你要推甄的學校全名、系所？有沒有分組？你要報哪一組？」
2. 「這個系所的備審資料上傳規定：總共要上傳幾個項目？每個項目幾頁？檔案大小限制？」
3. 「截止日期？」
4. 「有沒有需要針對特定教授的研究方向客製化？如果有，教授的名字和研究領域是？」
5. 「語言偏好：備審資料用中文還是英文？」

### Round 3 — 主打專題 🔥（15-20 題，含小故事）

這是**最重要的一輪**。請深入每一個細節。

#### 3a — 基本資訊（5 題）
1. 「你的主打專題（或作品）名稱是什麼？」
2. 「專題的起迄時間？總共開發幾天？」
3. 「團隊幾個人？其他成員叫什麼名字？他們分別負責什麼？」
4. 「你擔任的角色是什麼？」
5. 「有沒有 GitHub 連結或其他展示連結？」

#### 3b — 技術細節（5 題）
1. 「用了哪些硬體？請列出每一個零件的型號和用途。」
2. 「用了哪些軟體、程式語言、框架？」
3. 「有沒有用 Git？總 commits 數？分支策略？」
4. 「通訊協定（如果有的話）是怎麼設計的？版本演進過程？」
5. 「總共寫了多少行程式碼？」

#### 3c — 動機與問題（3 題）
1. 「為什麼想做這個專題？最初的動機是什麼？」
2. 「有沒有做過問卷調查？樣本數多少？具體結果數據？」
3. 「這個專題要解決的具體問題是什麼？」

#### 3d — 你的具體貢獻（5 題）
針對你負責的部分，逐項問清楚：
1. 「硬體方面：你設計了什麼電路？選了什麼 IC？接線怎麼規劃？」
2. 「軟體方面：你寫了什麼功能？UI 長怎樣？有哪些頁面？」
3. 「機構方面：你設計了什麼 3D 模型？用什麼軟體建模？什麼材料？什麼製程（3D 列印/雷切/CNC）？」
4. 「你覺得你貢獻最多的是哪一部分？」
5. 「這個專題有沒有得獎？什麼獎項？」

#### 3e — 困難故事（🔥 5 題，這是重點）

1. 「開發過程中最大的困難是什麼？請描述完整的經過。」
2. 「有沒有哪個時間點讓你特別印象深刻？請說說發生了什麼事。」
3. 「有沒有東西損壞、專案需要重來、或其他重大挫折的經驗？」
4. 「有沒有遇到技術/加工/製造方面的問題？你怎麼發現的？怎麼解決的？」
5. 「有沒有遇過檔案遺失、或需要從頭重建的經驗？當時你做了什麼？」

#### 3f — 反思與學習（3 題）
1. 「這個專題讓你學到最重要的三件事是什麼？」
2. 「你認為這個專題展現了你哪些能力？」
3. 「如果可以重來，你會改變什麼做法？」

#### 3g — 證明檔案（3 題）
1. 「請描述每一張專題相關的照片：檔名是什麼？照片裡拍了什麼？」
2. 「有沒有簡報檔、報告書、論文的 PDF？」
3. 「有沒有得獎證明、專利、或其他證明檔案？」

### Round 4 — 競賽獎項（每項 6 題）

如果有多項競賽，一項一項問。

1. 「競賽的全名？」
2. 「主辦單位？」
3. 「比賽日期？」
4. 「你得到什麼獎項/名次/分數？」
5. 「參賽規模？（多少隊伍/多少人參加）」
6. 「比賽方式（筆試/術科/作品審查）？」

### Round 5 — 證照與課外活動（每項 5 題）

先問證照，再問課外活動。

1. 「證照/活動的全名？」
2. 「發證/主辦單位？」
3. 「取得/參與日期？時數？」
4. 「你學到什麼？」
5. 「有沒有證明檔案（照片/掃描檔）？」

### Round 6 — 就讀動機與個人定位（5 題）

1. 「為什麼想念這個系？跟你的專題或經歷有什麼關聯？」
2. 「你認為你自己的三個關鍵能力或定位是什麼？（例如：跨域整合者、硬體實踐者、自主學習者）」
3. 「你對大學四年有什麼具體規劃？」
4. 「畢業後的長期目標？」
5. 「有沒有特別想跟審查教授說的話？」

### Round 7 — 實習科目與實作經驗（選填，如需要 B-1b 時才問）

如果使用者需要產出 B-1b（實習科目成果），加問以下問題：

1. 「你修過哪些實習課？（例如：車床、銑床、鉗工、焊接、電學）」
2. 「每門課你做了什麼作品？請列出每個作品。」
3. 「實習過程中學到最重要的技能或觀念是什麼？」
4. 「有沒有哪次實習讓你印象深刻？（例如：第一次操作機台的緊張、工件做壞的經驗）」
5. 「有沒有實習作品的照片？檔名是什麼？」

---

## 🟠 Phase 2：完整性檢查

在所有問答完成後，執行此檢查。

### MUST HAVE（缺少任一項 → 回到 Phase 1 補問）

| 欄位 | 檢查方式 |
|------|----------|
| 中文姓名 | profile.md → 1.1 |
| 英文姓名 | profile.md → 1.2 |
| 學校科系 | profile.md → 1.3—1.4 |
| 目標校系 | profile.md → 2.1—2.2 |
| 專題名稱 | profile.md → 3.1 |
| 專題時間 | profile.md → 3.2 |
| 你的角色 | profile.md → 3.4 |
| 貢獻（≥3 項） | profile.md → 6 |
| 困難故事（≥1 個） | profile.md → 7 |
| 競賽或證照（≥2 項） | profile.md → 10—11 |

### SHOULD HAVE（缺的話 → 加 TODO 繼續）

| 欄位 | 檢查方式 |
|------|----------|
| 專題反思 | profile.md → 8 |
| 就讀動機 | profile.md → 13 |
| 照片證明（≥5 張） | profile.md → 9 |

### 缺失處理方式

```python
# 如果缺少 MUST HAVE → 回到 Phase 1 補問
# 如果缺少 SHOULD HAVE → 在 build_ppt.py 中加註：
# TODO: 待補 - [欄位名稱] - 請使用者提供
```

---

## 🔵 Phase 3：修改 build_ppt.py

### 規則（嚴格遵守）

#### 規則 1：只使用 profile.md 現有資料
- 不發明任何分數、人名、日期、數據、技術細節
- 任何一段文字必須有 `# TRUTH: profile.md → 章節X.Y` 標記來源
- 如果 profile.md 缺某資料 → 不要自行填寫

#### 規則 2：缺失資料的處理
```python
# 範例：如果缺少比賽分數
# TRUTH: profile.md → 10.4 (競賽名次)
text = "{{AWARD_1_RANK}}"  # profile.md 有此資料
# TRUTH: profile.md → 10.5 (比賽分數) — 待補
# TODO: 待補 - 比賽分數 - 請使用者提供
```

#### 規則 3：圖片路徑
```python
IMG_PATH = os.path.join(IMAGES_DIR, "檔名.jpg")
# 不要寫死絕對路徑
```

#### 規則 4：圖片不存在時
```python
if not os.path.exists(img_path):
    print(f"[WARN] 圖片不存在: {img_path}")
    # TODO: 待補圖片 - 檔名
```

#### 規則 5：TRUTH 標記格式
```python
# TRUTH: profile.md → 章節編號.欄位名稱 (備註)
```

範例：
```python
# TRUTH: profile.md → 3.1 專題名稱
PROJECT_NAME = "{{PROJECT_NAME_ZH}}"

# TRUTH: profile.md → 6.2 硬體貢獻
text_lines = [
    "{{HW_CONTRIB_1}}",
    "{{HW_CONTRIB_2}}",
]
```

### 修改步驟

1. 讀取 `scripts/build_ppt.py` 全文
2. 找到所有 `{{PLACEHOLDER}}` 標記
3. 對照 profile.md 找到對應資料
4. 逐一取代，每次取代加上 `# TRUTH:` 註解
5. 如果 profile.md 無對應資料 → 保留 placeholder + 加 `# TODO: 待補`
6. 修改完成後再次讀取全文確認無遺漏 placeholder

### 各類別 PPT 對應的函式

build_ppt.py 已預先寫好各 A-D 類別的產生函式。根據使用者需要的類別，修改對應函式：

| 類別 | build_ppt.py 函式 | profile.md 資料來源 |
|------|-------------------|-------------------|
| B-1a | `build_project_ppt()` | 章節 3-9（專題） |
| B-1b | `build_internship_ppt()` | 章節 15（實習科目） |
| B-2a | `build_handdraft_ppt()` | 章節 16（手繪製圖） |
| B-2b | `build_cad_ppt()` | 章節 17（電腦製圖） |
| B-2c | `build_3dmodel_ppt()` | 章節 18（3D 建模） |
| C-1 | `build_flexible_ppt()` | 章節 19（彈性學習） |
| C-5 | `build_competition_ppt()` | 章節 10（競賽） |
| C-7a | `build_cert_pro_ppt()` | 章節 11（專業證照） |
| C-7b | `build_cert_other_ppt()` | 章節 11（其他證照） |
| C-8 | `build_achievement_ppt()` | 章節 12（課外活動） |
| D-1 | `build_overview_ppt()` | 章節 20（綜整心得） |
| D-2 | `build_motivation_ppt()` | 章節 13（就讀動機） |
| D-3 | `build_other_ppt()` | 章節 14（其他作品） |

在 `main()` 中，根據使用者需要的類別，開啟（取消註解）對應的函式呼叫。
不需要的類別就維持註解狀態。

---

## 🟣 Phase 4：執行

```bash
cd <project_root>
python scripts/build_ppt.py
```

如果執行失敗：
- 讀取錯誤訊息
- 修正問題（通常是圖片路徑錯誤或 python-pptx 未安裝）
- 重新執行

---

## 🔴 Phase 5：Truth Audit

### Step 5.1：掃描 TRUTH 標記
- 在 `scripts/build_ppt.py` 中搜尋所有 `# TRUTH:` 註解
- 對照 profile.md 確認每個 claim 都有來源
- 如果有 TRUTH 標記但對應的 profile 欄位為空 → 標記為 **FABRICATED**

### Step 5.2：掃描 TODO 缺失
- 搜尋所有 `# TODO: 待補` 註解
- 列出缺失清單

### Step 5.3：檢查圖片
- 掃描所有 `os.path.join(IMAGES_DIR, ...)` 的圖片路徑
- 確認每個檔案存在於 `images/`
- 列出缺失圖片

### Step 5.4：檢查輸出
- 確認 `output/` 目錄有產出 `.pptx` 檔案
- 每個檔案大小 > 0

### Step 5.5：回報結果

向使用者報告：

```
✅ 完成！以下是產出摘要：

📁 output/ 目錄產出：
  - B-1a_專題實作_主打作品.pptx (2.3 MB)
  - B-1b_實習成果.pptx (1.8 MB)
  - ...

⚠️ 缺失待補：
  - TODO: 比賽分數 (profile.md → 10.5)
  - TODO: 問卷圖 (images/問卷.png 不存在)

📋 Truth Audit：
  - 15 個 TRUTH claims 全數通過驗證
  - 0 個無來源 claims（優良）
  - 3 個 TODO 待補項目
```

---

## 🔄 迭代迴圈

任何 Phase 中，如果發現缺少關鍵資訊：

1. **暫停**當前動作
2. **記錄**缺少的欄位名稱與章節
3. **問使用者**（最多 5 題，避免疲勞）
4. **更新** profile.md
5. **繼續**原本 Phase

> 範例：Phase 3 修改 build_ppt.py 時，發現缺少專題的某個具體數據。
> → 暫停修改
> → 問：「你的專題開發期間總共多少天？Git 有多少 commits？」
> → 使用者回答後更新 profile.md → 對應章節
> → 繼續修改 build_ppt.py

---

## 🚫 反幻覺規則（最重要）

```
1. 你只能使用 profile.md 裡明確寫出的資訊。
2. 如果不確定，不要猜測，不要填寫。
3. 如果 profile.md 沒有某個資料：
   a. 在 build_ppt.py 中加入 # TODO: 待補 - [欄位名稱]
   b. 記錄到缺失清單
   c. 繼續處理其他部分
4. 絕對不虛構：
   ❌ 不存在的獎項、分數、人名
   ❌ 不存在的日期、時間、地點
   ❌ 不存在的技術細節、性能數據
   ❌ 不存在的照片或證明檔案
   ❌ 專案成果的誇大（例如「獲得全校第一名」→ 只能寫 profile 有的）
5. 永遠優先使用使用者提供的原文描述，
   不要為了「寫得漂亮」而改掉使用者的原意。
```

---

## 📋 Phase 完成總表

| Phase | 名稱 | 完成標誌 |
|-------|------|----------|
| 0 | 初始化 | 環境已檢查，profile 狀態已記錄 |
| 1 | 問卷 | 6 輪問答完成，profile.md 已更新 |
| 2 | 完整性檢查 | MUST HAVE 全數通過 |
| 3 | 修改 Python | 所有 placeholder 已處理，TRUTH 標記完成 |
| 4 | 執行 | build_ppt.py 成功執行，output/ 有檔案 |
| 5 | Truth Audit | 所有 claim 已驗證，結果已回報 |
